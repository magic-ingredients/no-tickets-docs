# Mintlify CLI build artifacts
.mintlify/
.next/

# Editor
.vscode/
.idea/
.DS_Store

# Node (Mintlify CLI dependencies if pinned locally)
node_modules/
npm-debug.log*
pnpm-debug.log*

# Generated sync artifacts (placeholder paths — wired in Tasks 2-4)
# Note: api-reference/openapi.json is committed (Pattern B —
# no-tickets-service pushes it on each release publish), so it is
# deliberately absent from this list.
cli-reference/generated/
mcp-tools/generated/
