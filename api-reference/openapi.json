{
  "openapi": "3.1.0",
  "info": {
    "title": "no-tickets API",
    "version": "0.0.0-placeholder",
    "description": "Placeholder spec. Replaced on each no-tickets-service release by the publish-openapi.yml workflow."
  },
  "servers": [
    {
      "url": "https://api.no-tickets.com",
      "description": "Production"
    }
  ],
  "paths": {
    "/health": {
      "get": {
        "summary": "Liveness probe",
        "description": "Placeholder operation so Mintlify's API reference tab renders before the first sync push from no-tickets-service lands.",
        "responses": {
          "200": {
            "description": "Service is up",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": ["status"],
                  "properties": {
                    "status": { "type": "string", "enum": ["ok"] }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
